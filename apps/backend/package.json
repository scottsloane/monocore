{
  "name": "@monocore/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "compile": "bun build --compile --target=bun src/index.ts --outfile ../desktop/src-tauri/binaries/monocore-backend"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
